Utility for elf to flash binary converter

Environment build (Linux or MSYS64)

	cd runner
	make sdk.build INSTALL_DIR=./build


Usage
./create_image.sh <inputfile.elf>

Windows
./create_image.bat <inputfile.elf>
